pacman::p_load(shiny,here,tidyr)

# run the app (step 1) ------------------------------------------------------
shiny::runApp(here::here("code/app_step1"))

# run the app (step 2) ------------------------------------------------------
shiny::runApp(here::here("code/app_step2"))

# run the app (step 3) ------------------------------------------------------
shiny::runApp(here::here("code/app_step3"))